numpy>=1.20.0
# optional on-disk backend (LMDB memory-mapped store):
lmdb>=1.4.0
# optional dense hybrid channel (local CPU embedder, ONNX, no torch):
fastembed>=0.3.0
# optional embedded vector DB (Alibaba Zvec, Proxima-based):
zvec>=0.5.0
# benchmarks only:
pyarrow>=12.0.0
